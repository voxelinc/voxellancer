#pragma once

namespace {
    enum BufferNames {
        Default = 0,
        Color,
        NormalZ,
        Emissisiveness,
        BlurTmp,
        Bloom,
        BufferCount // should always be last member and not be used as a name
    };
}
