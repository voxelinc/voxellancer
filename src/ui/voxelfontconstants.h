#pragma once

enum class FontSize {
    SIZE3x5,
    SIZE5x7
};

enum class FontAlign {
    LEFT,
    CENTER,
    RIGHT
};

enum class TextOrientation {
    BACKWARDS,
    SPHERE_STRAIGHT
};

enum class ButtonStyle {
    PLAIN,
    BORDERED,
    BORDERED_FILLED
};